{
  "name": "agentforge",
  "version": "0.1.0",
  "description": "AgentForge â€” an MCP-native blogging platform. MCP server + dashboard + public blog, one process, one backend.",
  "type": "module",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc -p tsconfig.json && node scripts/copy-migrations.mjs",
    "dev": "tsx watch src/server.ts",
    "start": "node --experimental-sqlite dist/server.js",
    "migrate": "tsx src/db/migrate.ts",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "cookie-session": "^2.1.0",
    "express": "^4.21.2",
    "marked": "^18.0.14",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/cookie-session": "^2.0.49",
    "@types/express": "^4.17.21",
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
